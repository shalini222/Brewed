export default function AdminPage({ setPage }) {
  return (
    <div
      style={{
        minHeight: "100vh",
        padding: "100px 30px",
        background: "#FDFAF5",
      }}
    >
      <button onClick={() => setPage("profile")}>
        ← Back
      </button>

      <h1>Brewed Admin Panel</h1>

      <p>Welcome, Admin ☕</p>
    </div>
  );
}
